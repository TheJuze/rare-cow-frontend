import { bananaSrc } from 'assets/img';
import { SearchItemProps } from './SearchItem';

export const searchItemPropsMocked: SearchItemProps = {
  name: 'Banana',
  filter: 'Ba',
  media: bananaSrc,
  disabled: false,
};
